<include src="../templates/header" />

<view>{{title}}</view>

<button type="default" hover-class="other-button-hover"> default </button>
<button type="primary" bindtap="primaryClick" hover-class="other-button-hover"> primary </button>

<text wx:if="{{show}}">{{text}}</text>

<view wx:for="{{news}}" wx:for-item="new">
    <text>{{index}} - {{new}}</text>
</view>

<import src="../templates/footer" />
<template is="footer01" />
